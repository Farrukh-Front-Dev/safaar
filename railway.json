{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build -w @safaar/types && npm run prisma:generate -w @safaar/backend && npm run build:backend -w @safaar/backend"
  },
  "deploy": {
    "startCommand": "cd apps/backend && npx prisma migrate deploy && NODE_OPTIONS='--no-network-family-autoselection' node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}