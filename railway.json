{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build -w @agoda/types && npm run prisma:generate -w @agoda/backend && npm run build:backend -w @agoda/backend"
  },
  "deploy": {
    "startCommand": "cd apps/backend && npx prisma migrate deploy && NODE_OPTIONS='--no-network-family-autoselection' node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}